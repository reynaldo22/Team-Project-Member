Admin = admin@bncc.net
        admin

user = test@email.com
        testing

field di db yang nullable (payment,verified,jawaban) dibuat manual lewat phpmyadmin
sql ada di data.sql

setelah login = url/home
kalo mau buka halaman admin = url/admin (ketik sendiri /admin nya)

roles users harus dibuat manual lewat admin/role
kalo blom dibuat user tersebut masih null (bukan user ataupun bukan admin)
